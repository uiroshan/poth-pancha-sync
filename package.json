{
  "name": "pothpancha-sync",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter \"./apps/**\" run dev",
    "deploy": "pnpm --filter \"./apps/**\" run deploy",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "vitest": "^4.0.18",
    "wrangler": "^4.103.0"
  }
}