export interface ProductSyncMessage {
  // Add product sync fields here
}

export interface ImageSyncMessage {
  // Add image sync fields here
}

export interface OrderUpdateMessage {
  // Add order update fields here
}
